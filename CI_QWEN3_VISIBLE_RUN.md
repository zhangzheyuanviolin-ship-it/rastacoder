# Observable Qwen3 baseline run

This branch exists only to trigger the repository's observable pull-request build for the exact known-good Qwen3 4B APK baseline. It changes no application source, model configuration, or runtime payload.
