# V16 full no-APK preflight trigger — package-schema-corrected rerun

Run the same locked no-APK gate after aligning the model-package validator with MLC's actual compiler-input schema: model_id in mlc-package-config and generated model_lib hashes in the runtime asset config. Functional source behavior is unchanged.
