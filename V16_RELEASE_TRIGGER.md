# V16 formal release trigger

The complete no-APK preflight run 33041993590 and exact MLC/native binary probe run 33041507127 are locked SUCCESS. Trigger exactly one stable-signed ARM64 APK build for version 0.0.15 (29), verify package/signature/ABI/MLC runtime/curl-cffi native companion and V9-V16 regressions, persist the verified source/handoff, then publish the GitHub Release.
