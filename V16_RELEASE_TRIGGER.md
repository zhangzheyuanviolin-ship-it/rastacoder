# V16 formal release trigger — registered workflow route

The previously successful full no-APK preflight run 33041993590 and exact MLC/native binary probe run 33041507127 are locked. Re-run the registered V16 preflight gate on this release commit, then execute the gated release-v16 job exactly once to build version 0.0.15 (29), verify the final ARM64 APK, persist verified V16 source/handoff, and publish the GitHub Release.
